# For more options and information see
# http://rptl.io/configtxt
# Some settings may impact device functionality. See link above for details

# Uncomment some or all of these to enable the optional hardware interfaces
#dtparam=i2c_arm=on
#dtparam=i2s=on
#dtparam=spi=on

# Audio deaktiviert (spart Ressourcen, kein Audio benötigt)
dtparam=audio=off

# Additional overlays and parameters are documented
# /boot/firmware/overlays/README

# Kamera/Display Auto-Detect deaktiviert (nicht benötigt, spart Bootzeit)
camera_auto_detect=0
display_auto_detect=0

# Automatically load initramfs files, if found
auto_initramfs=1

# Enable DRM VC4 V3D driver
dtoverlay=vc4-kms-v3d
max_framebuffers=2

# Firmware setzt keinen video= Parameter (KMS übernimmt selbst)
disable_fw_kms_setup=1

# Run in 64-bit mode
arm_64bit=1

# Disable compensation for displays with overscan
disable_overscan=1

# Run as fast as firmware / board allows
arm_boost=1

# USB Gadget Mode (dwc2 für Zero 2)
dtoverlay=dwc2

# Custom Resolution: Display nativ 480x1920
hdmi_group=2
hdmi_mode=87
hdmi_cvt=1920 480 60 6 0 0 0

# HDMI immer aktiv, auch ohne Hotplug-Signal
hdmi_force_hotplug=1

gpu_mem=128
[cm4]
# CM4 bleibt unverändert (nicht relevant für Zero 2)
otg_mode=1

[cm5]
dtoverlay=dwc2,dr_mode=host

[all]
